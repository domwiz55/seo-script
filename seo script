var keywordThemeParkRoblox = 8600;
var keywordThemeParkWorldwide = 6300;
var keywordThemeParkCrazy = 3800;

function moduloWords(keywordThemeParkRoblox, keywordThemeParkCrazy) {
  return keywordThemeParkRoblox % keywordThemeParkCrazy;
}
// compare a times keyword theme park roblox vs keword theme park crazy 
function scriptWordRunner(){
  console.log(moduloWords(keywordThemeParkRoblox,keywordThemeParkCrazy));
};
